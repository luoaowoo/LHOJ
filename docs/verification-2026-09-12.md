# 2026-09-12 验证记录

## 已完成

- `npm test`：7/7 通过。
  - Hydro native URL 幂等。
  - 工作区路径拒绝外部和协议相对地址。
  - 工作区链接与查询参数可以往返解析。
  - 比赛个人时间覆盖和题目顺序测试通过。
  - 所有前端功能链接均匹配应用路由。
- `npm run build`：通过。
- `dist/index.html` 引用的 5 个 JS/CSS 资源全部存在，无缺失。
- 主 bundle 不再包含旧的 `https://oj.luoaowoo.cn/register` 写法，包含 `/hydro-native`。
- Playwright 本地 smoke：`/`、`/login`、`/problems`、`/records`、`/contests`、`/training`、`/homework`、`/discuss`、`/ranking`、`/about` 均正常渲染，无 404。
- `/hydro?path=%2Flostpass` 正常在当前主题中渲染“忘记密码”表单。
- React 页面中已无 `target="_blank"` 打开 Hydro 原生功能页。
- 剩余 `hydroPublicUrl()` 只用于代码下载和题目文件下载，属于同源资源下载，不进入 Hydro 主题页面。

## 当前限制

- 801 实例的匿名用户无法访问主要业务页面，且 `/register` 当前返回 `302 /login`。
- 已提供 root 测试账号；只读页面和管理工作区已完成登录态验收，危险写操作尚未实际提交。
- 已完成 URL、解析、组件和构建级合同测试；权限写入测试必须使用测试账号继续执行。

## 提交

- `2adc6b2`：主题化 Hydro 工作区与迁移文档。
- `8e38278`：账户和讨论动作迁入站内工作区。
- `ea3b7f0`：题目和记录动作迁入站内工作区。
- `479d16b`：比赛、作业、训练动作迁入站内工作区。
- `47a196f`：完整 Hydro 动作覆盖。
- `86a16c6`：保留 Hydro 链接动作的 GET 方法。


## 全路由 404 审计

- 对 39 个当前使用的 `/hydro-native/*` 原生路径做匿名状态探测，未发现 404。
- 题目、比赛、作业、训练、讨论、记录、用户、账户和系统管理路由均返回正常的状态（登录跳转、权限拒绝或公开页面）。
- 所有 React 内部功能链接通过 `matchRoutes` 合同测试，不存在指向未声明页面的链接。
- `hydroNativeUrl` 已兼容绝对 Hydro URL，避免生成 `/hydro-native/http://...` 形式的错误地址。
- 工作区支持页面链接及相对链接跳转，锚点动作保留 GET 方法。

## 与 Hydro 的剩余差异

- `/register` 在 801 实例上服务端关闭，点击会由 Hydro 跳回登录；客户端无法绕过该限制。
- 复杂脚本型编辑器优先使用主题化工作区解析；如某插件页面无法完整解析，可通过工作区右上角“原生页面”打开同源 Hydro 页面完成全部操作。
- 已使用 root 测试账号完成页面和管理工作区验证；真实写操作仅做表单渲染/合同验证，未实际提交。


## 真实账号验证

- 使用普通登录表单完成 Hydro 会话登录，GraphQL 返回 `_id=232`、`role=root`。
- 通过本地 Vite 的 `/hydro-native` 代理验证：
  - 比赛详情、比赛题目列表、隐藏题详情均返回 200。
  - 比赛榜单、代码包、编辑、管理、气球、参赛选手均返回 200。
  - 题目编辑、数据配置、普通题解、统计、文件均返回 200。
  - 比赛上下文中的题解、统计、文件由 Hydro 明确禁用并返回 403；前端已隐藏这些入口，与 Hydro 一致。
  - 比赛代码和作业代码是 ZIP 下载，改为直接同源下载，不再送进 HTML 工作区解析。
- 本轮真实账号探测未发现 404。

## su 权限验证

- GraphQL 当前用户返回 UID `232`、`role=root`、`priv=-1`，确认属于 Hydro 超级用户。
- 前端新增 `isSuperUser()`，统一识别 `root`、`su`、`admin` 和 `priv=-1`，不再只判断 `root` 字符串。
- 登录完成并等待会话稳定后，以下站内工作区均成功渲染，无跳转：
  - 创建题目：5 个普通输入、2 个文本域。
  - 创建比赛：16 个普通输入、2 个文本域。
  - 创建作业：9 个普通输入、4 个文本域。
  - 创建训练：2 个普通输入、6 个文本域。
  - 系统仪表盘、用户权限、域资料、题目数据配置。
- `npx tsc --noEmit` 与 `npm run build` 均通过。